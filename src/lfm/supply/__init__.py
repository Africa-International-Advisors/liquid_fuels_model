"""Supply-side stub.

The xlsx is essentially demand-only. The brief says "supply-demand model",
so the supply layer needs to exist — even thin — to capture: SA refining
output, coastal imports, inland flows to BLNS, terminal/depot stocks.
v1 keeps this minimal; the slot is here so it can grow without restructuring.
"""
